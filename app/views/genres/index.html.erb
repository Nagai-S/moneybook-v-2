<h1>ジャンル一覧</h1>

<div class="account">
  <% if current_user.genres==[] %>
  <p>まずはこちらからジャンル(食費、給料など)の追加をしましょう。</p>
  <% end %>
  <div class="new_account">
    <li><%= link_to "ジャンル追加", new_user_genre_path(current_user) %></li>
  </div>
</div>

<div class="row">
 <div class="col-12 col-sm-6">
    <h2>支出</h2>
    <%= render "genre_table", iae: false %>
  </div>

  <div class="col-12 col-sm-6">
    <h2>収入</h2>
    <%= render "genre_table", iae: true %>
  </div> 
</div>