<h1><%= @card.name %></h1>

<h2>使用履歴</h2>
<% if @events.exists? %>
<%= render "events/table" %>
<% else %>
<p><%= @card.name %>を利用した履歴はありません。</p>
<% end %>

<h2>振替</h2>
<% if @axs.length > 0 %>
<%= render "account_exchanges/table" %>
<% else %>
<p><%= @card.name %>を利用した振替はありません。</p>
<% end %>

<h2>投資信託</h2>
<% if @fund_user_histories.exists? %>
<%= render "fund_user_histories/table" %>
<% else %>
<p><%= @card.name %>を利用した投資信託の売買はありません。</p>
<% end %>



